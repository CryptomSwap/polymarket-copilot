# Target definitions

Place per-target label generation logic here when needed (e.g. `labelSpreadAdjustedGoodDecision12h.ts`).
Current labels are implemented in `../build-labels.ts` and `@/lib/ml/shadow-dataset/build.ts`.
